<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <!-- Receiving card notifications (new Product / Carton card dispatched). -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- PRINTER MANAGER (2026-09-15): Bluetooth Classic SPP to the PM-241-BT
         thermal label printer. Version-gated permissions per task §8:
         legacy BLUETOOTH/ADMIN up to Android 11, BLUETOOTH_SCAN/CONNECT on
         12+ (scan declared neverForLocation — no location permission needed
         on 12+; FINE_LOCATION only for 11- discovery). -->
    <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation"
        tools:targetApi="s" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" android:maxSdkVersion="30" />
    <!-- Honeywell rugged devices (CT40): required by some firmwares to
         receive barcode broadcasts from the scanner service. Absent and
         harmless on normal phones. -->
    <uses-permission android:name="com.honeywell.decode" />

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <application
        android:name=".AyroviWorkerApplication"
        android:allowBackup="false"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:usesCleartextTraffic="false"
        android:theme="@style/Theme.AyroviWorker">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- Push receiver: started by the OS for a data message even when the
             app is backgrounded or fully closed, so a new receiving card
             always reaches the worker. Dormant until google-services.json
             is supplied (see docs/PUSH-SETUP.md). -->
        <service
            android:name=".push.AyroviMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>
        <!-- PRINTER MANAGER: loopback print bridge for the Admin web on this
             device (127.0.0.1 only; owns the single Bluetooth SPP link). -->
        <service
            android:name=".printer.PrintBridgeService"
            android:exported="false" />

        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="ayrovi_receiving" />

        <!-- Report photo capture: temp camera shots in the app cache. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>
</manifest>
