# Dependencies & build output
node_modules/
dist/
coverage/

# Local environment (contains real secrets — never commit)
.env

# Logs
*.log
npm-debug.log*

# OS / editor
.DS_Store
.idea/
.vscode/

# Isolated validation output (fixtures are source, execution data is not).
test-results/
