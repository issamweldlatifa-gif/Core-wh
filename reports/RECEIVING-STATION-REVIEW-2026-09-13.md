
## PRINT FIX — attempt 3: self-contained print WINDOW (fcd1b58, LIVE)
Owner-verified blank «Save as PDF» on Android Chrome for BOTH CSS approaches (visibility trick; portal+display root 8bc7328). Attempt 3 abandons app-CSS printing entirely: `printLabelsInNewWindow(codes)` opens a NEW TAB and `document.write`s a self-contained page whose labels are INLINE PNG data-URLs (CODE128 rendered offscreen via zxing→canvas, `code128DataUrl`), `@page{margin:10mm}`, label img 130mm×28mm + number beneath (v74 contract), `print()` 350ms after open; pop-up-blocked → alert hint. Applied to BOTH surfaces: admin TEMPORARY STORAGE → SHELF LABELS → PRINT LABELS, and BATCHES → Print label. Gates: tsc clean, vite build OK, 142/142. CI 34793486737 SUCCESS; prod health `fcd1b58`. FIXED only when owner confirms the new-tab preview shows barcode+number.

## PRINT FIX — attempt 4: ROOT CAUSE found & fixed (3eacafe, LIVE)
Owner: print button DEAD («معاش يستجيب»). Reproduced in CI: **@zxing/library ships NO CODE_128 writer** — `MultiFormatWriter().encode(v, BarcodeFormat.CODE_128, …)` throws `IllegalArgumentException: No encoder available for format 4` on EVERY device. Attempt 3's `code128DataUrl` (and the batch drawCode128 path) called it BEFORE window.open → uncaught exception → silent dead button. Fix (3eacafe): self-contained CODE128B encoder — pattern table GENERATED from `Code128Reader.CODE_PATTERNS` (scanner-proven decode table); 8 vitest tests incl. round-trip DECODE of every production label (K1, K20, ST-INBOUND-02, batch codes) against that same table. Labels are inline SVG (no canvas, no images, nothing device-fragile); `window.open` runs FIRST (tap gesture; pop-up blocked → hint alert); any failure closes the tab and alerts `Print error: <real message>` — silent failure impossible; auto-print fires at 400ms and 1600ms from the opener; callers (TemporaryStorage, Batches) wrap handlers in try/catch with the same alert. Gates: tsc clean, build OK, 150/150. CI 34794645320 SUCCESS; prod `3eacafe`; served chunks byte-verified (print-sheet-BEU22XeR.js + TemporaryStorage-Dl_XbMHc.js contain encoder + alerts). **OWNER-CONFIRMED WORKING 2026-09-14 («انه يعمل جميل») — printing saga CLOSED** (4 attempts: visibility CSS ✗, portal+display CSS ✗, PNG data-URL ✗ dead button, SVG+self-contained window ✓).

## CYCLE 87 (1bc926e) — CONTINUOUS RECEIVING SCAN LOOP + PROFESSIONAL VERDICT MARKS
Owner scope 2026-09-14: «تحديث يخص فقط نسخه ct40 و ادمين واب» — CT40 + admin web ONLY (phone code paths shared builds unchanged behaviourally except the shared verdict mark visuals; phone stays frozen, no install instructions). versionCode 87 (1.8.0-ct40.9).
- SCAN-EXIT ROOT CAUSE (code-proven): the RECEIVING verdict auto-rearm (1200ms) called ScanVerdict.onBack, and ReceivingHomeScreen wired onBack = dismiss + onExitSession → after EVERY successful scan the worker was thrown to the MAIN home. All other stations use onBack = dismiss only (code-compared across Batch/Packing/Shipping/Sorting/TempStorage/Trace). FIX (9629bfc): verdict dismiss NEVER exits; exits = camera ✕ / system BACK (cameraBackEndsTheSession still green). CI caught the v1.7.5 test encoding the old behaviour (redVerdictBackEndsTheSession, failed phone+ct40) → updated to redVerdictBackDismissesStaysInSession (1bc926e): BACK dismisses, READY_TO_SCAN returns, onExitSession NOT raised.
- VERDICT MARKS redesigned («علامه خضراء … اكثر احترافيه»): in-camera mark = dark contrast disc + white ring + drawn-stroke ✓/✕ with overshoot scale-in (spring-ish CubicBezier), no flat glyph; overlay mark = halo+ring sweep, single path-trimmed ✓ (no doubled glyph), settle glow (AnimatedVerdictMark rewritten, 900ms).
- Sandbox gradle TLS-blocked (JVM handshake) — gates ran on CI: run 34797421687 SUCCESS (native compile+lint+core; emulator phone+ct40). Artifact ayrovi-worker-receiving-pilot-1bc926e… (10330471774); ct40 APK zip-valid 80,155,798 B, sha256 18205327a5dcad9d…, release `ct40-apk-1bc926e` (id 388110668) — CT40 asset ONLY (owner scope), download byte-exact vs artifact. Rollback: `ct40-apk-8786be6` (86).
- Master push e4e6816..1bc926e → backend deploy; prod health `1bc926e` (builtAt 02:14:58Z). Incident: sandbox snapshot rolled local branch behind the pushed commit (73b41bc parented on e4e6816) → rebuilt linearly 9629bfc→1bc926e (diff vs 9629bfc = test file only), no history rewrite, fast-forward push.

## ADMIN BATCH 2 (0a6b935, LIVE) — deletes + complete worker form + TS squares + responsive
Owner orders 2026-09-14: admin device delete («نجمو نحذفو اي جهاز»), batch hard delete anytime («ينجم يحذف ويتحكم بيهم»), complete the worker-creation form («مهام نتاعو ناقصه كمل حط مهام»), TS sections as squares («اجعلها مثل قسم مربعات»), responsive TS admin.
- DEVICES: DELETE /v1/devices/:idOrCode (stations.manage) — live sessions revoked first, audited `DEVICE_REMOVED`; Devices page red "remove" + confirm. FK relations are ON DELETE SET NULL (verified in schema) → clean row deletion.
- BATCHES: DELETE /v1/batches/:id (batch.void, ANY status) — audited `BATCH_HARD_DELETED`; BatchItems cascade (schema BatchItem.batch onDelete: Cascade); no other table holds a Batch FK (receiving lines store batchCode as strings — verified). Batches page Delete button + permanent-delete modal; action-matrix tests updated (delete available in every status; VOID ≠ delete).
- WORKER FORM: station select + TASK_CATALOG checklist; after createWorker → assignStation + workerTaskCreate per task (title = task label, backend allows taskKey+title without entity — verified in assignments.service create()).
- TS SQUARES: overview now returns per-station `containerList` (id/code/section/stored/capacity); STATIONS·SECTIONS card renders per-section square grids — fill-colored (grey empty / amber partial / red full), fill bar + tooltip; responsive admin CSS (<860px: tables scroll, modals fit, grids collapse).
- Migration 20260914120000_audit_device_removed_batch_hard_deleted: two additive AuditAction enum values (same pattern as the login-throttled migration). Gates: backend tsc + 282/282; frontend tsc + build + 150/150. CI 34801656609 SUCCESS; prod `0a6b935`.
- GIT INCIDENT: a sandbox rollback staged pre-v87 mobile files into the first batch-2 commit (would have regressed cycle 87). Repaired before push: reset to origin/master (e1757d5) + re-applied ONLY the 16 batch-2 files → linear 0a6b935; versionCode 87 verified intact; nothing pushed that was not validated. Docs commit for that aborted commit never pushed (no trace on remote).

## ADMIN BATCH 3 (fbef925, LIVE) — Control/Operations on the NEW workflow + TRIAL RESET + polish
- PIPELINE (§4B, additive): new BATCH stage FIRST (active / in-receiving / done — real batch counts incl. new RECEIVING_COMPLETED counter) and TEMPORARY STORAGE stage after RECEIVING (containers / stored / review — real TS counts). Renders in Contrôle AND Operations (shared Pipeline component). Old stages untouched below TS (outbound chain).
- TRIAL RESET (owner: «اعمل restart للبينات تجربيه كل», scope agreed: operational data ONLY): POST /v1/operations/data-control/trial-reset (operations.correct; reason ≥3 chars + exact word RESET enforced server-side). ONE transaction, FK-safe order (schema inventory: the ONLY Restrict into the wipe set is BatchItem→AyroviUnit, cleared by deleting Batch first — everything else Cascade/SetNull). WIPES: batches+items+units+customers, arrivals(+items), receiving sessions/products/reports/cartons/scan-events/discrepancies/worker-logs, TS containers/items/reports, putaway+placements, product station moves, corrections, exceptions, task assignments, warehouse cartons, operational containers, article units. KEEPS: users/devices/sessions/stations/warehouse structure/product master/settings/AUDIT. Audited `DATA_TRIAL_RESET` (migration 20260914130000, additive) with per-table counts in metadata; DataControl red card shows the scope + typed confirm + the removed-counts result.
- POLISH: hover lift on buttons, focus rings, card hover elevation, sticky table headers, tag brightness — zero logic touched.
- Gates: backend tsc + 282/282; frontend tsc + build + 150/150 (also re-run on the REBASED tree after the conflict resolution). CI 34802472654 SUCCESS (pre-rebase content = same files; the rebase only re-based topology onto 0a6b935/758a7f4 with 2 trivial conflicts resolved: schema enum tail + css EOF append — both verified by the post-rebase gate run). Prod health `fbef925` db up (migration applied at boot); unauth probe of the new endpoint = 401 (route registered).
- GIT INCIDENT (same class as batch 2): sandbox rollback detached batch-3 from batch-2's commits → first master push rejected (non-fast-forward). Fixed by rebase onto origin/master (758a7f4), conflicts resolved by keeping BOTH sides (batch-2 enum values + batch-3 DATA_TRIAL_RESET; responsive CSS + polish CSS), post-rebase full gates re-run, then fast-forward push. No history rewrite; nothing forced.

## ADMIN BATCH 4 (d6701e4, LIVE) — pipeline main path = NEW workflow + role cleanup
Owner screenshots 2026-09-14 (TS squares CONFIRMED working — K1 4/20 amber) + 3 notes:
1. «opretion pepeline … تستعمل في داتا قديمه»: the poll was already live (shell §15, 30s) — the stale READING came from the ARRIVAL + RECEIVING CONTAINER/TOTE stages counting OLD CRM-flow records on the main strip. Pipeline now renders MAIN = BATCH → RECEIVING → TEMPORARY STORAGE → outbound; the two legacy stages moved into a collapsed «LEGACY FLOW» section (display-only split, API untouched; counts remain real). Header states the new chain + auto-refresh.
2. «DONE DONE» label duplication on stage 01 BATCH → cell unit 'done'→'batches' (renders DONE BATCHES).
3. «في worck محطات جديده معناش بش نزيدو عمال جدد»: Add-Worker form now offers ONLY the 5 station roles (RECEIVING/SORTING/PUTAWAY/PACKING/SHIPPING_WORKER); legacy/compat (INBOUND_WORKER, PICKER, PACKER) stay in the SYSTEM for existing users but are not offered at creation (flagged `legacy: true` in WORKER_ROLE_OPTIONS).
Gates: backend tsc + 282/282; frontend tsc + build + 150/150. CI 34832511686 SUCCESS; prod `d6701e4` db up. Git: sandbox rollback hit AGAIN at turn start (HEAD detached at e4e6816, batch-2/3 missing locally) → recovery recipe applied FIRST (re-add origin + fetch + reset --hard origin/master 4b4549c) before any edit; zero content loss.
