# =============================================================================
# AYROVI Warehouse Core — Phase 0 — root environment template
# Each app reads its own variables (see backend/.env.example, frontend/.env.example).
# This root template is for local development orchestration. Never commit real secrets.
# =============================================================================

# --- Backend ---
DATABASE_URL="postgresql://ayrovi:ayrovi_dev@127.0.0.1:5432/ayrovi_warehouse?schema=public"
PORT=3000
NODE_ENV=development
CORS_ORIGINS="http://localhost:5173"
JWT_ACCESS_SECRET=dev_access_secret_change_me_in_production_0001
JWT_REFRESH_SECRET=dev_refresh_secret_change_me_in_production_0002
JWT_ACCESS_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d
INITIAL_ADMIN_CODE=ADMIN001
INITIAL_ADMIN_PASSWORD=ChangeMe!2024

# --- Frontend ---
VITE_API_BASE=/api
VITE_API_PROXY_TARGET=http://localhost:3000
